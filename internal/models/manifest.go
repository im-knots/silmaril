package models

import (
	"github.com/silmaril/silmaril/pkg/types"
)

// Re-export types for backward compatibility
type ModelManifest = types.ModelManifest
type InferenceHints = types.InferenceHints
type ModelFile = types.ModelFile